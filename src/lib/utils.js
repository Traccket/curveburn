/**
 * Stub de utilidades. Actualmente el proyecto no necesita el helper `cn()`
 * porque las clases se construyen con template literals directos en los
 * componentes. Si llegas a requerirlo, instala:
 *
 *   npm install clsx tailwind-merge
 *
 * Y descomenta:
 *
 *   import { clsx } from 'clsx';
 *   import { twMerge } from 'tailwind-merge';
 *   export function cn(...inputs) { return twMerge(clsx(inputs)); }
 */
export {};
